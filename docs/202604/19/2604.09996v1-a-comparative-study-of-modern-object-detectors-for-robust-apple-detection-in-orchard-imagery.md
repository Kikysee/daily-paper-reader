---
title: A Comparative Study of Modern Object Detectors for Robust Apple Detection in Orchard Imagery
title_zh: 现代目标检测器用于果园图像中鲁棒苹果检测的比较研究
authors: "Mohammed Asad, Ajai Kumar Gautam, Priyanshu Dhiman, Rishi Raj Prajapati"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09996v1"
tags: ["query:q8"]
score: 9.0
evidence: 果园图像中的苹果检测用于作物监测
tldr: 准确的苹果检测对果园自动化至关重要，但复杂环境带来了巨大挑战。本文在AppleBBCH81数据集上建立了一个受控的评估基准，公平比较了YOLO11n、RT-DETR-L等六种现代目标检测器。结果表明YOLO11n定位精度最高，YOLOv10n综合F1分数最佳。研究强调，检测器的选择需综合考量定位精度、阈值鲁棒性及下游任务需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09996v1/fig-001.webp\", \"caption\": \"Fig. 4. Object-level confusion summary for YOLO11n on the validation split at IoU = 0.5 and confidence ≥ 0.05 (TP = 1465, FP = 920, FN = 46; TN is not defined for object detection).\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-002.webp\", \"caption\": \"Fig. 3. Precision-recall curves for all models on the validation split at IoU = 0.5.\", \"page\": 6, \"index\": 2, \"width\": 527, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-003.webp\", \"caption\": \"Fig. 1. Proposed workflow for apple detection on AppleBBCH81.\", \"page\": 3, \"index\": 3, \"width\": 527, \"height\": 348}]"
motivation: 果园环境中的光照变化、遮挡和密集分布使苹果检测面临挑战，且现有研究缺乏公平可重复的比较基准。
method: 在公开数据集AppleBBCH81上建立统一的评估基准，对比了YOLOv10n、YOLO11n、RT-DETR-L等六种主流目标检测器的性能表现。
result: YOLO11n在严格定位精度上表现最佳，YOLOv10n在固定阈值下获得最高F1分数，而RT-DETR-L召回率高但低置信度下误检较多。
conclusion: 果园目标检测器的选择不仅应关注定位精度，还需综合考虑阈值鲁棒性及具体下游任务的需求。
---

## 摘要
果园图像中准确的苹果检测对于产量预测、果实计数、机器人采摘和作物监测非常重要。然而，光照变化、枝叶杂乱、密集的果簇以及部分遮挡使得检测变得困难。为了提供公平且可重复的比较，本研究在公开的 AppleBBCH81 数据集上建立了一个用于单类苹果检测的受控基准，采用确定的训练、验证和测试集划分，并在六个代表性检测器（YOLOv10n、YOLO11n、RT-DETR-L、Faster R-CNN (ResNet50-FPN)、FCOS (ResNet50-FPN) 和 SSDLite320 (MobileNetV3-Large)）上使用了统一的评估协议。性能评估主要采用 COCO 标准的 mAP@0.5 和 mAP@0.5:0.95，并利用精确率-召回率曲线以及在 IoU = 0.5 时的固定阈值精确率、召回率和 F1 分数进一步分析与阈值相关的行为。在验证集上，YOLO11n 取得了最佳的严格定位性能（mAP@0.5:0.95 = 0.6065，mAP@0.5 = 0.9620），RT-DETR-L 和 YOLOv10n 紧随其后。在置信度 >= 0.05 的固定操作点上，YOLOv10n 获得了最高的 F1 分数，而 RT-DETR-L 由于在低置信度下存在大量假阳性，虽然实现了极高的召回率，但精确率较低。这些发现表明，果园部署的检测器选择不仅应以定位感知准确率为指导，还应考虑阈值鲁棒性以及下游任务的需求。

## Abstract
Accurate apple detection in orchard images is important for yield prediction, fruit counting, robotic harvesting, and crop monitoring. However, changing illumination, leaf clutter, dense fruit clusters, and partial occlusion make detection difficult. To provide a fair and reproducible comparison, this study establishes a controlled benchmark for single-class apple detection on the public AppleBBCH81 dataset using one deterministic train, validation, and test split and a unified evaluation protocol across six representative detectors: YOLOv10n, YOLO11n, RT-DETR-L, Faster R-CNN (ResNet50-FPN), FCOS (ResNet50-FPN), and SSDLite320 (MobileNetV3-Large). Performance is evaluated primarily using COCO-style mAP@0.5 and mAP@0.5:0.95, and threshold-dependent behavior is further analyzed using precision-recall curves and fixed-threshold precision, recall, and F1-score at IoU = 0.5. On the validation split, YOLO11n achieves the best strict localization performance with mAP@0.5:0.95 = 0.6065 and mAP@0.5 = 0.9620, followed closely by RT-DETR-L and YOLOv10n. At a fixed operating point with confidence >= 0.05, YOLOv10n attains the highest F1-score, whereas RT-DETR-L achieves very high recall but low precision because of many false positives at low confidence. These findings show that detector selection for orchard deployment should be guided not only by localization-aware accuracy but also by threshold robustness and the requirements of the downstream task.

---

## 论文详细总结（自动生成）

以下是对该论文的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在精准农业中，果园图像中的苹果检测是产量预测、果实计数、机器人采摘和作物监测的基础任务。
*   **核心问题**：真实的果园环境极其复杂，存在光照变化、枝叶遮挡、果实密集簇生等问题，导致检测器容易出现漏检或重复检测。尽管深度学习在水果检测上取得了进展，但现有研究往往使用不同的数据集、划分方法、模型架构和评估指标（如仅报告 mAP@0.5），导致不同检测器之间缺乏公平、可重复的对比。
*   **整体含义**：本文旨在建立一个严格受控的基准（Benchmark），在统一的评估协议下，公平比较现代主流目标检测器在果园复杂环境中的表现。研究强调，评估检测器不仅要看理论上的定位精度，更要看其在实际部署阈值下的鲁棒性。

### 2. 论文提出的方法论
*   **核心思想**：构建一个标准化的工作流，包含统一的数据集划分、标准化的预处理、一致的训练配置以及多维度的评估协议，以消除不同研究间因实验设置不同而产生的偏差。
*   **关键技术细节与流程**：
    *   **任务定义**：单类别目标检测（仅检测“苹果”）。
    *   **数据标准化**：将 YOLO 格式标签统一转换为绝对像素坐标；**保留无苹果的负样本图像**（空标签），以有效评估和减少模型的假阳性（误检）偏差。
    *   **匹配规则（One-to-One Matching）**：在评估时采用贪心匹配策略，按置信度降序排列预测框，每个预测框只能匹配一个未被匹配过的真实框（IoU $\ge$ 0.5），防止对同一个苹果的重复检测被重复计为正确。
    *   **多维评估指标**：
        *   *定位感知指标*：采用 COCO 标准的 mAP@0.5 和 mAP@0.5:0.95。
        *   *部署导向指标*：在固定操作点（IoU = 0.5 且 置信度 $\ge$ 0.05）下计算精确率（Precision）、召回率（Recall）和 F1 分数。
        *   *阈值诊断*：绘制精确率-召回率（PR）曲线，计算 PR AUC，并引入 95% Wilson 置信区间来量化统计不确定性。

### 3. 实验设计
*   **数据集/场景**：使用公开的果园图像数据集 **AppleBBCH81**。包含 1838 张图像和 15309 个苹果边界框标注（包含 16 张无苹果的负样本图像）。
*   **数据划分**：采用固定的确定性划分，约为 80/10/10 比例（训练集 1470 张，验证集 183 张，测试集 185 张）。所有定量结果均在验证集上报告。
*   **对比方法（6种代表性模型）**：
    1.  **YOLOv10n**：现代轻量级单阶段 CNN 基线。
    2.  **YOLO11n**：最新的 Ultralytics 单阶段检测器。
    3.  **RT-DETR-L**：基于 Transformer 的实时检测器。
    4.  **Faster R-CNN (ResNet50-FPN)**：经典双阶段检测器基线。
    5.  **FCOS (ResNet50-FPN)**：无锚框（Anchor-free）单阶段基线。
    6.  **SSDLite320 (MobileNetV3-Large)**：面向边缘设备部署的超轻量级基线。

### 4. 资源与算力
*   **硬件配置**：实验在 Google Colab 环境下进行，使用了一张 **NVIDIA A100-SXM4-40GB GPU**。
*   **软件环境**：Python 3.12.12, PyTorch 2.10.0+cu128, CUDA 12.8。
*   **训练时长与参数**：文中未明确说明具体的训练耗时（小时数），但给出了明确的训练设置：Ultralytics 模型（YOLO 系列、RT-DETR）训练了 50 个 Epoch（Batch size = 16）；Torchvision 模型（Faster R-CNN, FCOS, SSDLite）训练了 10 个 Epoch（Batch size = 4）。所有模型均基于 COCO 预训练权重进行微调。

### 5. 实验数量与充分性
*   **实验数量**：对 6 种不同架构的模型进行了完整的训练与验证，提取了 mAP 指标、固定阈值下的 TP/FP/FN 绝对数量、Precision/Recall/F1 分数，并绘制了 PR 曲线。
*   **充分性与客观性**：实验设计**非常客观且公平**。作者严格控制了随机种子（seed=0），使用了完全一致的数据集划分和评估代码。特别是在评估中引入了“一对一匹配规则”和“负样本保留”，有效避免了传统评估中容易掩盖的重复检测问题。结合理论指标（mAP）和实际部署指标（固定阈值 F1），使得对比极具说服力。

### 6. 论文的主要结论与发现
*   **严格定位精度最高**：在 COCO 标准评估下，**YOLO11n** 表现最好（mAP@0.5:0.95 = 0.6065，mAP@0.5 = 0.9620），RT-DETR-L 和 YOLOv10n 紧随其后。
*   **实际部署表现（固定阈值）最佳**：在置信度 $\ge$ 0.05 的实际操作点下，**YOLOv10n** 实现了精确率和召回率的最佳平衡，获得了最高的 F1 分数（0.7808）。
*   **高 mAP 不等于高实用性**：RT-DETR-L 虽然 mAP 很高且召回率极高（0.9702），但在低置信度下产生了海量的假阳性（误检），导致精确率暴跌至 0.2246。YOLO11n 在该阈值下的误检也多于 YOLOv10n。
*   **核心指导意义**：果园目标检测器的选择不能仅看单一的 mAP 指标，必须结合下游任务需求。例如，果实计数任务对误检（假阳性）非常敏感，而机器人采摘则更看重高召回率和抗遮挡能力。

### 7. 优点（亮点）
*   **评估维度的创新**：打破了仅依赖 mAP 评价模型的惯例，引入了面向实际农业部署的固定阈值诊断（Fixed-Threshold Diagnostics），直击果园应用中“重复检测”和“漏检”的痛点。
*   **严谨的统计学支撑**：在目标检测评估中引入了 95% Wilson 置信区间，为精确率和召回率提供了统计学上的不确定性估计，增加了结论的可靠性。
*   **细节处理到位**：保留空标签图像以测试模型的抗干扰能力，这在很多刷榜论文中常被忽略，但对真实农业场景至关重要。

### 8. 不足与局限
*   **数据集覆盖有限**：实验仅在一个公开数据集（AppleBBCH81）上进行，缺乏跨果园、跨季节、跨苹果品种的泛化能力验证。
*   **缺乏端侧部署的真实数据**：虽然论文强调了“面向部署（Deployment-oriented）”，但并未提供模型在边缘设备上的实际推理延迟（FPS）、内存占用或模型大小等物理指标对比。
*   **训练配置的微小差异**：由于框架限制，Ultralytics 模型（50 Epochs, Batch 16）与 Torchvision 模型（10 Epochs, Batch 4）的训练轮数和批次大小不同。虽然这符合各自框架的最佳实践，但在绝对算力消耗上存在一定的不对等。

（完）
