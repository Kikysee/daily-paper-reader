---
title: A Comparative Study of Modern Object Detectors for Robust Apple Detection in Orchard Imagery
title_zh: 面向果园图像鲁棒苹果检测的现代目标检测器比较研究
authors: "Mohammed Asad, Ajai Kumar Gautam, Priyanshu Dhiman, Rishi Raj Prajapati"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09996v1"
tags: ["query:q8"]
score: 9.0
evidence: 用于作物监测和产量预测的苹果检测
tldr: 准确的苹果检测对果园自动化至关重要，但受复杂环境制约。本文在AppleBBCH81数据集上建立了统一的评估基准，对比了YOLO11n、RT-DETR-L等六种现代目标检测器。结果显示YOLO11n定位精度最高，YOLOv10n的F1分数最优。该研究为果园检测器的选择提供了重要参考，强调需综合考虑定位精度、阈值鲁棒性及下游任务需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09996v1/fig-001.webp\", \"caption\": \"Fig. 4. Object-level confusion summary for YOLO11n on the validation split at IoU = 0.5 and confidence ≥ 0.05 (TP = 1465, FP = 920, FN = 46; TN is not defined for object detection).\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-002.webp\", \"caption\": \"Fig. 3. Precision-recall curves for all models on the validation split at IoU = 0.5.\", \"page\": 6, \"index\": 2, \"width\": 527, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-003.webp\", \"caption\": \"Fig. 1. Proposed workflow for apple detection on AppleBBCH81.\", \"page\": 3, \"index\": 3, \"width\": 527, \"height\": 348}]"
motivation: 果园环境中的光照变化、遮挡和密集分布使得苹果检测面临挑战，且现有研究缺乏公平可重复的基准比较。
method: 在公开数据集AppleBBCH81上建立统一的评估基准，对比了YOLOv10n、YOLO11n、RT-DETR-L等六种现代目标检测器的性能表现。
result: YOLO11n在严格定位精度上表现最佳，YOLOv10n在固定阈值下获得最高F1分数，而RT-DETR-L召回率高但低置信度下误检较多。
conclusion: 果园目标检测器的选择不仅应关注定位精度，还需综合考虑阈值鲁棒性及具体下游任务的需求。
---

## 摘要
在果园图像中准确检测苹果对于产量预测、果实计数、机器人采摘和作物监测具有重要意义。然而，光照变化、枝叶杂乱、果簇密集以及部分遮挡给检测带来了困难。为了提供公平且可重复的比较，本研究在公开的 AppleBBCH81 数据集上建立了一个单类别苹果检测的受控基准，采用确定性的训练集、验证集和测试集划分，并对六种代表性检测器（YOLOv10n、YOLO11n、RT-DETR-L、Faster R-CNN (ResNet50-FPN)、FCOS (ResNet50-FPN) 和 SSDLite320 (MobileNetV3-Large)）使用了统一的评估协议。性能评估主要采用 COCO 标准的 mAP@0.5 和 mAP@0.5:0.95，并利用精确率-召回率曲线以及在 IoU = 0.5 时的固定阈值精确率、召回率和 F1 分数，进一步分析了与阈值相关的行为。在验证集上，YOLO11n 取得了最佳的严格定位性能（mAP@0.5:0.95 = 0.6065，mAP@0.5 = 0.9620），RT-DETR-L 和 YOLOv10n 紧随其后。在置信度 >= 0.05 的固定工作点下，YOLOv10n 获得了最高的 F1 分数，而 RT-DETR-L 由于在低置信度下存在大量假阳性（误检），虽然召回率极高但精确率较低。这些发现表明，在果园部署中选择检测器时，不仅应考虑定位感知准确率，还应综合考虑阈值鲁棒性以及下游任务的具体需求。

## Abstract
Accurate apple detection in orchard images is important for yield prediction, fruit counting, robotic harvesting, and crop monitoring. However, changing illumination, leaf clutter, dense fruit clusters, and partial occlusion make detection difficult. To provide a fair and reproducible comparison, this study establishes a controlled benchmark for single-class apple detection on the public AppleBBCH81 dataset using one deterministic train, validation, and test split and a unified evaluation protocol across six representative detectors: YOLOv10n, YOLO11n, RT-DETR-L, Faster R-CNN (ResNet50-FPN), FCOS (ResNet50-FPN), and SSDLite320 (MobileNetV3-Large). Performance is evaluated primarily using COCO-style mAP@0.5 and mAP@0.5:0.95, and threshold-dependent behavior is further analyzed using precision-recall curves and fixed-threshold precision, recall, and F1-score at IoU = 0.5. On the validation split, YOLO11n achieves the best strict localization performance with mAP@0.5:0.95 = 0.6065 and mAP@0.5 = 0.9620, followed closely by RT-DETR-L and YOLOv10n. At a fixed operating point with confidence >= 0.05, YOLOv10n attains the highest F1-score, whereas RT-DETR-L achieves very high recall but low precision because of many false positives at low confidence. These findings show that detector selection for orchard deployment should be guided not only by localization-aware accuracy but also by threshold robustness and the requirements of the downstream task.

---

## 论文详细总结（自动生成）

以下是对该论文的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在精准农业中，苹果的自动检测是产量预测、果实计数、机器人采摘和作物监测的基础任务。然而，真实的果园环境极其复杂，面临光照变化、枝叶杂乱、果簇密集以及果实被部分遮挡等严峻挑战。
*   **核心问题**：尽管深度学习在果实检测中取得了显著进展，但现有文献中的研究往往使用不同的数据集、数据划分方法、模型架构和评估指标。特别是许多研究仅报告 `mAP@0.5`，这掩盖了模型在更严格定位要求下的缺陷，也无法反映检测器在不同置信度阈值下的真实表现。
*   **整体含义**：本文旨在解决上述“难以公平比较”的问题，通过在一个公开数据集上建立一个受控的、可重复的统一基准（Benchmark），对当前主流的现代目标检测器进行公平对比，从而为果园实际部署提供科学的选型指导。

### 2. 论文提出的方法论
*   **核心思想**：不提出新的模型架构，而是建立一个标准化的评估流程（统一的数据划分、预处理和评估协议），以测试不同架构家族的检测器在严格定位和实际部署阈值下的表现。
*   **关键技术细节与流程**：
    *   **任务定义**：单类别（苹果）目标检测。
    *   **模型选择**：涵盖6种不同架构的代表性模型：YOLOv10n、YOLO11n（现代单阶段CNN）、RT-DETR-L（实时Transformer）、Faster R-CNN（双阶段基线）、FCOS（无锚框单阶段）、SSDLite320（轻量级移动端基线）。
    *   **评估协议**：
        *   **COCO标准指标**：使用 `mAP@0.5` 和严格的 `mAP@0.5:0.95` 评估整体定位与排序质量。
        *   **一对一匹配规则**：按置信度降序排列预测框，每个预测框最多匹配一个真实的苹果（IoU $\ge$ 0.5），防止同一苹果被重复计算。
        *   **阈值依赖指标**：在固定的实际操作点（IoU = 0.5 且 置信度 $\ge$ 0.05）下计算精确率（Precision）、召回率（Recall）和 F1 分数，并绘制 PR 曲线及计算 PR AUC。

### 3. 实验设计
*   **数据集**：使用公开的果园数据集 **AppleBBCH81**（包含 1,838 张图像和 15,309 个 YOLO 格式的苹果边界框标注，包含部分无苹果的负样本图像）。
*   **数据划分**：采用固定的确定性划分，约为 80/10/10（训练集 1470 张，验证集 183 张，测试集 185 张）。所有结果均在验证集上报告。
*   **对比方法（Baselines）**：YOLOv10n, YOLO11n, RT-DETR-L, Faster R-CNN (ResNet50-FPN), FCOS (ResNet50-FPN), SSDLite320 (MobileNetV3-Large)。

### 4. 资源与算力
*   **硬件算力**：明确指出实验在 **Google Colab** 上运行，使用了一张 **NVIDIA A100-SXM4-40GB GPU**。
*   **软件环境**：Python 3.12.12, PyTorch 2.10.0+cu128, CUDA 12.8。
*   **训练时长与参数**：Ultralytics 框架模型（YOLO系列、RT-DETR）训练了 50 个 Epoch（Batch size = 16）；Torchvision 框架模型（Faster R-CNN, FCOS, SSDLite）训练了 10 个 Epoch（Batch size = 4）。所有模型均基于 COCO 预训练权重进行微调。

### 5. 实验数量与充分性
*   **实验数量**：在统一的数据集和划分下，对 6 种不同架构的模型进行了完整的训练、验证，并输出了多维度的评估指标（mAP、固定阈值下的 TP/FP/FN 统计、PR 曲线、95% Wilson 置信区间）。
*   **客观性与公平性**：**非常高**。本文的核心贡献就是“公平”。通过固定随机种子（seed=0）、统一数据划分、统一预处理逻辑以及统一的评估代码，消除了以往研究中因实验设置不同带来的偏差。
*   **充分性**：在单一数据集上的横向对比非常充分，但缺乏跨数据集（如不同果园、不同季节）的泛化性实验。

### 6. 论文的主要结论与发现
*   **严格定位性能（mAP）**：**YOLO11n** 表现最好（mAP@0.5:0.95 = 0.6065），RT-DETR-L 和 YOLOv10n 紧随其后。轻量级的 SSDLite320 表现最差。
*   **实际部署性能（固定阈值 F1 分数）**：在置信度 $\ge$ 0.05 的低阈值操作点下，**YOLOv10n** 实现了最佳的精确率与召回率平衡，F1 分数最高（0.780870）。
*   **阈值鲁棒性差异**：高 mAP 并不意味着实际部署最好。例如，RT-DETR-L 虽然 mAP 很高且召回率极高（0.97），但在低置信度下会产生海量的假阳性（误检），导致精确率暴跌至 0.22。YOLO11n 的误检也多于 YOLOv10n。
*   **核心指导意义**：果园检测器的选择不能仅看单一的 mAP 指标，必须结合下游任务（如“果实计数”极度厌恶重复检测/误检）和模型的阈值鲁棒性来综合决定。

### 7. 优点
*   **评估视角的创新**：打破了唯 `mAP` 论，引入了面向实际工程部署的“固定阈值诊断（Fixed-Threshold Diagnostics）”和一对一贪婪匹配，这对于农业机器人采摘和计数极具实际指导价值。
*   **高度的标准化**：为混乱的农业视觉检测领域提供了一个极其规范、可复现的 Benchmark 范式。
*   **统计严谨性**：在报告固定阈值指标时，引入了 95% Wilson 置信区间来量化精确率和召回率的不确定性，增加了结论的可靠性。

### 8. 不足与局限
*   **数据集覆盖有限**：仅在单一数据集（AppleBBCH81）上进行了测试，未验证模型在不同果园、不同光照季节、不同苹果品种上的泛化能力。
*   **缺乏真实的端侧部署测试**：虽然文章多次强调“面向部署（Deployment-oriented）”，但并未实际测试和对比这些模型在边缘设备（如树莓派、Jetson Nano）上的推理延迟（FPS）、内存占用和功耗。
*   **任务局限**：仅进行了单类别检测，未涉及果园环境中同样重要的多类别检测（如区分成熟/未成熟果实、树枝避障）或实例分割任务。

（完）
