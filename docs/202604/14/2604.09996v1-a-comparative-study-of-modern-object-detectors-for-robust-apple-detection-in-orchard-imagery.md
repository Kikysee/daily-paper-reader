---
title: A Comparative Study of Modern Object Detectors for Robust Apple Detection in Orchard Imagery
title_zh: 果园图像中用于鲁棒苹果检测的现代目标检测器比较研究
authors: "Mohammed Asad, Ajai Kumar Gautam, Priyanshu Dhiman, Rishi Raj Prajapati"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09996v1"
tags: ["query:q8"]
score: 9.0
evidence: 果园图像中的苹果检测，用于作物监测和产量预测
tldr: 针对果园环境中苹果检测面临的光照变化和遮挡等挑战，本文在AppleBBCH81数据集上建立了一个受控基准，对比评估了YOLOv10n、YOLO11n等六种现代目标检测器。研究详细分析了各模型在不同评估指标下的表现，为实际果园部署中的检测器选择提供了重要指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09996v1/fig-001.webp\", \"caption\": \"Fig. 4. Object-level confusion summary for YOLO11n on the validation split at IoU = 0.5 and confidence ≥ 0.05 (TP = 1465, FP = 920, FN = 46; TN is not defined for object detection).\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-002.webp\", \"caption\": \"Fig. 3. Precision-recall curves for all models on the validation split at IoU = 0.5.\", \"page\": 6, \"index\": 2, \"width\": 527, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-003.webp\", \"caption\": \"Fig. 1. Proposed workflow for apple detection on AppleBBCH81.\", \"page\": 3, \"index\": 3, \"width\": 527, \"height\": 348}]"
motivation: 果园环境复杂（如光照变化、遮挡等）导致苹果检测困难，且缺乏对现代目标检测器在统一标准下的公平比较。
method: 在公开数据集上建立统一的划分和评估协议，对比分析了六种代表性目标检测器（如YOLO11n、RT-DETR-L等）的mAP和阈值相关性能。
result: YOLO11n在严格定位指标上表现最佳，YOLOv10n在固定阈值下取得最高F1分数，而RT-DETR-L表现出高召回低精度的特点。
conclusion: 果园部署的检测器选择不仅应关注定位精度，还需综合考虑阈值鲁棒性以及具体下游任务的需求。
---

## 摘要
果园图像中准确的苹果检测对于产量预测、果实计数、机器人采摘和作物监测至关重要。然而，光照变化、树叶杂乱、密集的果簇以及部分遮挡使得检测变得困难。为了提供公平且可重复的比较，本研究在公开的AppleBBCH81数据集上建立了一个受控的单类苹果检测基准，使用确定性的训练、验证和测试划分，并在六个具有代表性的检测器（YOLOv10n、YOLO11n、RT-DETR-L、Faster R-CNN (ResNet50-FPN)、FCOS (ResNet50-FPN) 和 SSDLite320 (MobileNetV3-Large)）上采用统一的评估协议。性能评估主要使用COCO风格的mAP@0.5和mAP@0.5:0.95，并使用精确率-召回率曲线以及在IoU = 0.5时的固定阈值精确率、召回率和F1分数进一步分析依赖于阈值的行为。在验证集划分上，YOLO11n实现了最佳的严格定位性能，mAP@0.5:0.95 = 0.6065，mAP@0.5 = 0.9620，紧随其后的是RT-DETR-L和YOLOv10n。在置信度 >= 0.05的固定操作点上，YOLOv10n获得了最高的F1分数，而RT-DETR-L由于在低置信度下存在许多假阳性，实现了非常高的召回率但精确率较低。这些发现表明，果园部署的检测器选择不仅应以定位感知准确率为指导，还应考虑阈值鲁棒性和下游任务的需求。

## Abstract
Accurate apple detection in orchard images is important for yield prediction, fruit counting, robotic harvesting, and crop monitoring. However, changing illumination, leaf clutter, dense fruit clusters, and partial occlusion make detection difficult. To provide a fair and reproducible comparison, this study establishes a controlled benchmark for single-class apple detection on the public AppleBBCH81 dataset using one deterministic train, validation, and test split and a unified evaluation protocol across six representative detectors: YOLOv10n, YOLO11n, RT-DETR-L, Faster R-CNN (ResNet50-FPN), FCOS (ResNet50-FPN), and SSDLite320 (MobileNetV3-Large). Performance is evaluated primarily using COCO-style mAP@0.5 and mAP@0.5:0.95, and threshold-dependent behavior is further analyzed using precision-recall curves and fixed-threshold precision, recall, and F1-score at IoU = 0.5. On the validation split, YOLO11n achieves the best strict localization performance with mAP@0.5:0.95 = 0.6065 and mAP@0.5 = 0.9620, followed closely by RT-DETR-L and YOLOv10n. At a fixed operating point with confidence >= 0.05, YOLOv10n attains the highest F1-score, whereas RT-DETR-L achieves very high recall but low precision because of many false positives at low confidence. These findings show that detector selection for orchard deployment should be guided not only by localization-aware accuracy but also by threshold robustness and the requirements of the downstream task.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：在精准农业中，果园图像中的苹果检测对于产量预测、果实计数、机器人采摘和作物监测至关重要。
*   **核心问题**：真实的果园环境极其复杂，存在光照变化、树叶遮挡、果实密集簇生等问题，导致检测器容易漏检或产生重复检测。尽管深度学习在水果检测中取得了进展，但现有研究通常使用不同的数据集、划分方法和评估指标，缺乏在统一标准下对现代目标检测器进行公平、可重复的比较。
*   **整体含义**：本文旨在建立一个受控的基准测试（Benchmark），在统一的数据集划分和评估协议下，客观比较六种不同架构的现代目标检测器在果园苹果检测任务中的表现，为实际农业部署提供指导。

### 2. 论文提出的方法论
*   **核心思想**：不提出新的网络架构，而是构建一个标准化的评估工作流（Workflow），以确保不同检测器家族之间的公平比较。该工作流强调不仅要看传统的定位精度（mAP），还要关注特定置信度阈值下的实际部署表现（如F1分数）。
*   **关键技术细节与流程**：
    1.  **数据标准化**：将所有标注统一为单类（Apple）检测任务，并转换为各框架所需的格式（如绝对像素坐标）。保留无苹果的负样本图像以减少假阳性偏差。
    2.  **统一训练设置**：所有模型均从COCO预训练权重初始化，使用固定的随机种子（seed=0）以保证可重复性。
    3.  **多维度评估协议**：
        *   **COCO风格指标**：使用mAP@0.5和mAP@0.5:0.95评估模型在不同置信度下的整体排序和定位精度。
        *   **固定阈值指标**：在IoU=0.5且置信度≥0.05的固定操作点下，使用一对一贪婪匹配规则计算精确率（Precision）、召回率（Recall）和F1分数，以模拟实际部署（如计数或采摘）时的表现。
        *   **PR曲线与AUC**：通过精确率-召回率曲线分析模型对阈值变化的鲁棒性。

### 3. 实验设计
*   **数据集**：使用了公开的果园数据集 **AppleBBCH81**。经过预处理后包含1838张图像和15309个苹果边界框。
*   **数据划分**：采用固定的确定性划分，约按80/10/10的比例分为训练集（1470张）、验证集（183张）和测试集（185张）。所有定量结果均在验证集上报告。
*   **对比方法（Baselines）**：涵盖了六种代表性架构：
    1.  **YOLOv10n**：现代轻量级单阶段CNN。
    2.  **YOLO11n**：最新的Ultralytics单阶段检测器。
    3.  **RT-DETR-L**：基于Transformer的实时检测器。
    4.  **Faster R-CNN (ResNet50-FPN)**：经典的两阶段检测器基线。
    5.  **FCOS (ResNet50-FPN)**：无锚框（Anchor-free）单阶段基线。
    6.  **SSDLite320 (MobileNetV3-Large)**：面向边缘设备部署的超轻量级基线。

### 4. 资源与算力
*   **硬件环境**：实验在 Google Colab 上进行，使用了一张 **NVIDIA A100-SXM4-40GB GPU**。
*   **软件环境**：Python 3.12.12, PyTorch 2.10.0+cu128, Ultralytics 框架等。
*   **训练时长与参数**：Ultralytics模型（YOLO系列、RT-DETR）训练50个Epoch，批次大小为16；Torchvision模型（Faster R-CNN等）训练10个Epoch，批次大小为4。具体训练时长文中未明确列出，但给出了完整的超参数配置。

### 5. 实验数量与充分性
*   **实验数量**：主要进行了一组核心的横向对比实验，即6个模型在同一个数据集划分上的全面性能评估（包含mAP对比、固定阈值下的TP/FP/FN统计、PR曲线绘制）。
*   **充分性与客观性**：实验设计非常客观和公平。所有模型使用完全相同的训练/验证数据划分，且评估代码统一，消除了因数据泄露或评估脚本不同带来的偏差。然而，实验仅在一个数据集上进行，缺乏跨数据集或跨季节的泛化性测试，也未包含针对特定模块的消融实验。

### 6. 论文的主要结论与发现
*   **严格定位性能**：在COCO风格的mAP指标下，**YOLO11n** 表现最佳（mAP@0.5:0.95 = 0.6065），RT-DETR-L和YOLOv10n紧随其后。现代实时检测器显著优于传统的两阶段或轻量级基线。
*   **实际部署表现（固定阈值）**：在置信度≥0.05的实际操作点下，排名发生变化。**YOLOv10n** 实现了最高的F1分数（0.780870），在精确率和召回率之间取得了最佳平衡。
*   **高mAP不等于低误报**：尽管RT-DETR-L的mAP很高且召回率极高（0.970218），但在低置信度下产生了大量假阳性（FP=5061），导致精确率极低。YOLO11n的假阳性也多于YOLOv10n。
*   **核心指导意义**：果园部署的检测器选择不能仅看单一的mAP指标，必须结合下游任务（如计数对假阳性敏感，采摘对召回率敏感）和阈值鲁棒性来综合决定。

### 7. 优点
*   **标准化的公平比较**：打破了以往农业检测论文“自说自话”的局面，提供了一个严格控制变量的Benchmark，极大地提高了研究的可信度和可重复性。
*   **贴近实际应用的评估体系**：引入了固定阈值下的F1分数和具体的TP/FP/FN统计，指出了高mAP模型在实际低阈值部署时可能面临的“假阳性爆炸”问题，这对农业机器人落地具有很强的指导价值。
*   **统计严谨性**：在固定阈值指标中引入了95% Wilson置信区间，增强了结果的统计学意义。

### 8. 不足与局限
*   **数据集单一**：仅在AppleBBCH81一个数据集上进行了测试，未能验证模型在不同果园、不同光照季节或不同水果品种上的泛化能力。
*   **缺乏端侧硬件实测**：虽然讨论了部署问题，但并未在实际的边缘设备（如树莓派、Jetson Nano）上测试推理延迟（FPS）、内存占用和功耗，仅停留在理论指标层面。
*   **未探讨特定优化**：作为基准测试，本文使用了各模型的标准实现，未针对果园遮挡或小目标问题引入特定的农业领域优化策略（如注意力机制、特殊数据增强等）。

（完）
