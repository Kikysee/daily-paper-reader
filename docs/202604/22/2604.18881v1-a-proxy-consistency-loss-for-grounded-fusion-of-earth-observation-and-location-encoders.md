---
title: A Proxy Consistency Loss for Grounded Fusion of Earth Observation and Location Encoders
title_zh: 用于地球观测与位置编码器锚定融合的代理一致性损失
authors: "Zhongying Wang, Kevin Lane, Levi Cai, Morteza Karimzadeh, Esther Rolf"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18881v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 地球观测与代理变量融合以解决稀疏标签问题
tldr: 针对地球观测任务中高质量标签稀缺的问题，本文提出了一种代理一致性损失（PCL）方法。该方法利用可训练的位置编码器，将大量易获取的地理代理变量作为地理先验进行整合与正则化。在空气质量预测和贫困测绘实验中，该方法有效吸收了代理数据信息，不仅提升了样本内的预测精度，还显著增强了模型在无标签区域的泛化能力，表现优于传统的融合策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18881v1/fig-001.webp\", \"caption\": \"Figure 4. In the air quality task we show spatial impacts of EPA-only training (left) and PCL training (right) on different days-of-year. The EPA-only model overfits on EPA-station locations, whereas the PCL model is better regularized, with spatially smoother embedding features.\", \"page\": 7, \"index\": 1, \"width\": 492, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.18881v1/fig-002.webp\", \"caption\": \"Figure 2. Performance (R2) on air quality task across checkerboard sizes δ (◦). As δ increases, performance generally degrades. Curves show the mean over 8 checkerboard partitions (4 spatial offsets × train/test swap), and shaded regions denote ±1 SE across partitions.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.18881v1/fig-003.webp\", \"caption\": \"Table 1. Performance summary for the air quality task on the UAR 50/50 spatial split and the systematically offset checkerboard split (δ = 8◦). For UAR 50/50, metrics are reported as mean over 5 random seeds ± standard error (SE); proxy-only regression is deterministic (no SE). For checkerboard, we evaluate each method over 8 partitions (4 spatial offsets × train/test swap) and report mean ± SE across partitions. Best results are in bold, second best are italicized.\", \"page\": 6, \"index\": 3, \"width\": 1032, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.18881v1/fig-004.webp\", \"caption\": \"Table 2. In poverty mapping, PCL and heuristics targets in Africa with a spatio-temporal location encoder results in the best performance. Best results are in bold, second best are italicized. Performance is averaged out over the course of 3 randomly seeded runs. PCL results train on patch-level heuristics, predicting a tensor of min, max, mean, and centroid pixel values of a 128x128 VIIRS image patch centered on the sample’s latitude and longitude.\", \"page\": 6, \"index\": 4, \"width\": 487, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.18881v1/fig-005.webp\", \"caption\": \"Figure 5. In the air quality task the EPA-only model fails to capture finer-grain temporal trends. The PCL-trained model instead shows temporal trends are dominant, as shown in Table 1, whereas the EPA-only model has larger spatial variance.\", \"page\": 8, \"index\": 5, \"width\": 446, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.18881v1/fig-006.webp\", \"caption\": \"Figure 1. Our model architecture fuses a trainable location encoder (blue) with an EO-based encoder model for prediction of a supervised learning task y, while the location encoder must also be able to construct related proxy tasks z. Our proposed proxy consistency loss is incorporated in a multi-task loss, where the primary prediction task is a location-grounded prediction task (green), and an auxiliary proxy task (purple) is predicted using just the location encoder outputs. The overall objective function minimized during training is a weighted combination of the standard prediction loss and the proxy consistency loss which regularizes the location encoder.\", \"page\": 3, \"index\": 6, \"width\": 1036, \"height\": 323}]"
motivation: 地球观测领域的监督学习常受限于高质量标签数据的稀疏性，但现实中存在大量与目标变量相关的地理代理数据可供利用。
method: 提出一种代理一致性损失（PCL），通过可训练的位置编码器将丰富的代理变量作为地理先验进行整合与正则化。
result: 在空气质量预测和贫困测绘任务中，该方法在样本内和样本外预测性能上均优于现有的输入融合和冻结位置嵌入策略。
conclusion: PCL能有效吸收代理数据中的丰富信息，且学习到的潜在嵌入显著提升了模型在无标签区域的泛化能力。
---

## 摘要
以地球观测数据为输入的监督学习通常受到高质量标注数据或实地测量数据（用作训练标签）稀疏性的限制。随着地理数据产品的日益丰富，在许多情况下，存在一些与目标变量相关但又不同的变量可以被利用。我们通过一个可训练的位置编码器将这些代理变量整合到地理先验中，并引入了一种代理一致性损失（PCL）公式，将代理数据融入位置编码器中。我们方法背后的第一个关键见解是，将位置编码器作为一种敏捷且灵活的方式，从大量可用的代理数据中进行学习，这些代理数据的采样可以独立于训练标签的可用性。我们的第二个关键见解是，我们需要对位置编码器进行适当的正则化，以在有限的标注数据下实现良好的性能和鲁棒性。我们在空气质量预测和贫困制图方面的实验表明，通过位置编码器隐式整合代理数据，优于将两者同时作为观测编码器输入的方法，也优于使用冻结的预训练位置嵌入作为地理先验的融合策略。样本内预测的卓越性能表明，PCL能够吸收来自代理变量的丰富信息；而样本外预测的卓越性能则表明，学习到的潜在嵌入有助于泛化到没有训练标签的区域。

## Abstract
Supervised learning with Earth observation inputs is often limited by the sparsity of high-quality labeled or in-situ measured data to use as training labels. With the abundance of geographic data products, in many cases there are variables correlated with - but different from - the variable of interest that can be leveraged. We integrate such proxy variables within a geographic prior via a trainable location encoder and introduce a proxy consistency loss (PCL) formulation to imbue proxy data into the location encoder. The first key insight behind our approach is to use the location encoder as an agile and flexible way to learn from abundantly available proxy data which can be sampled independently of training label availability. Our second key insight is that we will need to regularize the location encoder appropriately to achieve performance and robustness with limited labeled data. Our experiments on air quality prediction and poverty mapping show that integrating proxy data implicitly through the location encoder outperforms using both as input to an observation encoder and fusion strategies that use frozen, pretrained location embeddings as a geographic prior. Superior performance for in-sample prediction shows that the PCL can incorporate rich information from the proxies, and superior out-of-sample prediction shows that the learned latent embeddings help generalize to areas without training labels.

---

## 论文详细总结（自动生成）

以下是对该论文的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在地球观测（Earth Observation, EO）领域的机器学习任务中（如环境监测、农业经济分析），高质量的实地测量数据或标注数据通常极其稀疏且获取成本高昂。
*   **研究动机**：现实中存在大量与预测目标相关但并不完全一致的“代理数据”（Proxy Data，如低分辨率的再分析数据、夜间灯光数据等）。现有方法要么简单地将代理数据作为输入通道叠加（收益有限），要么使用冻结的预训练位置编码器（难以适配特定任务）。如果直接解冻并训练特定任务的位置编码器，又极易对稀疏的训练点产生空间过拟合。
*   **核心问题**：如何有效地利用广泛存在的代理数据，来训练一个既能适配特定任务，又具备强大地理泛化能力（不过拟合于稀疏标签位置）的位置编码器？

### 2. 论文提出的方法论
*   **核心思想**：提出一种**代理一致性损失（Proxy Consistency Loss, PCL）**，构建一个多任务学习框架。将代理数据作为一种空间正则化手段，通过要求位置编码器能够重建代理变量，来赋予其全局的地理先验知识。
*   **模型架构**：
    *   **观测编码器（Observation Encoder）**：处理卫星图像等观测输入，输出观测嵌入 $e_{obs}$。
    *   **位置编码器（Location Encoder）**：处理经度、纬度（及时间）输入，输出位置嵌入 $e_{loc}$。
    *   **融合与预测**：将 $e_{obs}$ 和 $e_{loc}$ 拼接后输入主预测头，输出主任务预测值 $\hat{y}$。
*   **关键技术细节与公式**：
    *   **代理预测分支**：仅使用位置嵌入 $e_{loc}$ 输入到一个辅助预测头中，尝试重建代理变量 $z$，得到预测值 $\hat{z}$。
    *   **损失函数**：总损失为监督预测损失与代理一致性损失的加权和：$L = L_{pred} + \lambda L_{proxy-consistency}$。其中 $L_{proxy-consistency} = \|\hat{z} - z\|_2^2$。
    *   **独立采样策略**：由于代理数据在整个研究区域内广泛存在，模型在训练时可以脱离稀疏标签的限制，在整个时空域内**均匀随机采样（UAR）**代理数据。定义采样率 $\rho$，即每个包含 $B$ 个有标签样本的批次中，额外抽取 $\rho B$ 个无标签的代理样本用于计算 PCL。

### 3. 实验设计
*   **数据集与场景**：
    1.  **空气质量（PM2.5）预测**：覆盖美国本土（CONUS）。主目标为 EPA 监测站的真实 PM2.5 数据；观测输入为卫星气溶胶和气象特征；**代理变量**为 NCAR 的 12km 分辨率 PM2.5 再分析数据。
    2.  **贫困测绘（Poverty Mapping）**：基于 SustainBench，覆盖非洲。主目标为 DHS 资产指数；观测输入为 Landsat 卫星图像；**代理变量**为 VIIRS 夜间灯光数据的图像块统计特征。
*   **Benchmark（数据划分）**：
    *   **UAR 50/50 划分**：随机划分 50% 站点训练，50% 测试（测试空间插值/样本内性能）。
    *   **棋盘格划分（Checkerboard split）**：按 $\delta$ 度的网格划分，交替作为训练集和测试集（严格测试空间外推/分布外 OOD 泛化性能）。
*   **对比方法**：
    *   仅使用代理数据的线性回归。
    *   仅使用观测编码器（无位置信息）。
    *   将代理数据作为额外输入通道叠加（Proxy-stacked）。
    *   融合冻结的预训练位置编码器（GeoCLIP, Climplicit）。
    *   两阶段代理预训练（先用代理数据预训练位置编码器，再冻结并训练主任务）。
    *   不带 PCL 的可训练位置编码器融合。

### 4. 资源与算力
*   **文中提及的资源**：论文提到了训练的超参数，如 AdamW 优化器、学习率（3e-4 / 1e-4）、批次大小（256 / 128）、训练轮数（100 epoch / 50 epoch），以及每个 epoch 的训练耗时（例如在 PM2.5 任务中，带 PCL 的模型约需 21秒/epoch，不带 PCL 约需 18秒/epoch）。
*   **未明确说明的信息**：论文**没有明确说明**使用了何种型号的 GPU（如 A100、RTX 3090 等）、GPU 的具体数量以及总的算力消耗（GPU Hours）。

### 5. 实验数量与充分性
*   **实验数量**：进行了 2 个截然不同的地理任务实验；在 PM2.5 任务中测试了不同棋盘格大小（$\delta$ 从 1.5° 到 8°）的外推性能；进行了丰富的消融实验（包括改变采样率 $\rho$、对比仅在站点采样与全局随机采样、是否加入时间编码、是否引入额外的气象辅助代理变量等）。
*   **充分性与客观性**：实验设计**非常充分且客观公平**。棋盘格划分严格评估了地理模型的痛点（空间泛化）；对比基线涵盖了当前主流的融合策略（直接叠加、预训练大模型 GeoCLIP 等）；消融实验清晰地证明了 PCL 及其全局随机采样策略的有效性。

### 6. 论文的主要结论与发现
*   **性能全面提升**：通过 PCL 隐式整合代理数据，在样本内（插值）和样本外（外推）预测中均显著优于直接叠加代理数据或使用冻结预训练位置编码器的方法。
*   **缓解空间过拟合**：PCA 可视化表明，仅用稀疏标签训练的位置编码器会在站点周围产生伪影（严重过拟合），而 PCL 训练出的位置嵌入在空间上更加平滑，具有更好的时空结构。
*   **全局采样的重要性**：PCL 的成功高度依赖于在无标签区域的广泛采样。如果仅在有标签的站点位置计算 PCL，几乎没有性能提升；全局均匀随机采样是提升地理泛化能力的关键驱动力。
*   **联合训练优于预训练**：端到端的 PCL 联合训练在应对严苛的空间外推（大 $\delta$ 棋盘格）时，比两阶段的代理预训练表现更鲁棒。

### 7. 优点
*   **视角创新**：没有将代理数据简单视为输入特征，而是将其作为位置编码器的“空间正则化器”，优雅地解决了位置编码器在稀疏标签下的过拟合问题。
*   **解耦采样机制**：允许代理数据的采样独立于真实标签的分布，实现了一种低成本的“全域监督（everywhere supervision）”。
*   **轻量且高效**：相比于预训练庞大的地理基础模型，该方法仅需增加一个辅助预测头和少量计算开销（每轮增加约 3 秒），即可实现针对特定任务的高效定制。

### 8. 不足与局限
*   **数据依赖性**：该方法的前提是必须存在一个与主任务相关且具有全局（或大范围）覆盖的代理数据集。在某些特定任务中，寻找高质量的代理数据可能很困难。
*   **偏差继承风险（Bias Inheritance）**：强制位置编码器的潜在空间与代理变量保持一致，可能会导致模型学习并放大代理数据中固有的偏差。例如，如果再分析数据在野火期间系统性地低估了 PM2.5，PCL 模型也可能继承这一缺陷。
*   **超参数敏感性**：需要仔细调节损失权重 $\lambda$ 和采样率 $\rho$，以平衡主任务和代理任务，这增加了模型调优的成本。

（完）
