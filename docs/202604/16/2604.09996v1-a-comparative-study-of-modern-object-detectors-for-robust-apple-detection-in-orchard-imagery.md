---
title: A Comparative Study of Modern Object Detectors for Robust Apple Detection in Orchard Imagery
title_zh: 面向果园图像鲁棒苹果检测的现代目标检测器比较研究
authors: "Mohammed Asad, Ajai Kumar Gautam, Priyanshu Dhiman, Rishi Raj Prajapati"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09996v1"
tags: ["query:q8"]
score: 9.0
evidence: 果园苹果检测用于作物监测和产量预测
tldr: 准确的苹果检测对果园管理至关重要，但复杂环境给检测带来挑战。本文在AppleBBCH81数据集上建立统一基准，公平对比了YOLO11n、YOLOv10n等六种现代目标检测器。结果显示YOLO11n定位精度最高，YOLOv10n的F1分数最优。研究表明，果园检测器的选择需综合考量定位精度、阈值鲁棒性及下游任务需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09996v1/fig-001.webp\", \"caption\": \"Fig. 4. Object-level confusion summary for YOLO11n on the validation split at IoU = 0.5 and confidence ≥ 0.05 (TP = 1465, FP = 920, FN = 46; TN is not defined for object detection).\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-002.webp\", \"caption\": \"Fig. 3. Precision-recall curves for all models on the validation split at IoU = 0.5.\", \"page\": 6, \"index\": 2, \"width\": 527, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.09996v1/fig-003.webp\", \"caption\": \"Fig. 1. Proposed workflow for apple detection on AppleBBCH81.\", \"page\": 3, \"index\": 3, \"width\": 527, \"height\": 348}]"
motivation: 针对果园复杂环境下苹果检测面临的光照变化和遮挡等挑战，当前缺乏对现代目标检测器公平且可重复的性能评估。
method: 在公开数据集AppleBBCH81上建立统一的训练测试划分和评估协议，对比分析了YOLOv10n、YOLO11n等六种代表性目标检测器。
result: YOLO11n在严格定位精度上表现最佳，YOLOv10n获得最高F1分数，而RT-DETR-L召回率极高但低置信度下误检较多。
conclusion: 果园目标检测器的选择不仅应关注定位精度，还需综合考虑阈值鲁棒性以及具体下游任务的实际需求。
---

## 摘要
在果园图像中准确检测苹果对于产量预测、果实计数、机器人采摘和作物监测具有重要意义。然而，光照变化、枝叶杂乱、果簇密集以及部分遮挡给检测带来了困难。为了提供公平且可重复的比较，本研究在公开的 AppleBBCH81 数据集上建立了一个单类别苹果检测的受控基准，采用确定性的训练集、验证集和测试集划分，并对六种代表性检测器（YOLOv10n、YOLO11n、RT-DETR-L、Faster R-CNN (ResNet50-FPN)、FCOS (ResNet50-FPN) 和 SSDLite320 (MobileNetV3-Large)）使用了统一的评估协议。性能评估主要采用 COCO 标准的 mAP@0.5 和 mAP@0.5:0.95，并利用精确率-召回率曲线以及在 IoU = 0.5 时的固定阈值精确率、召回率和 F1 分数，进一步分析了与阈值相关的行为。在验证集上，YOLO11n 取得了最佳的严格定位性能（mAP@0.5:0.95 = 0.6065，mAP@0.5 = 0.9620），RT-DETR-L 和 YOLOv10n 紧随其后。在置信度 >= 0.05 的固定工作点下，YOLOv10n 获得了最高的 F1 分数，而 RT-DETR-L 由于在低置信度下存在大量假阳性，虽然召回率极高但精确率较低。这些发现表明，在果园部署中选择检测器时，不仅应以定位感知准确率为指导，还应综合考虑阈值鲁棒性以及下游任务的具体需求。

## Abstract
Accurate apple detection in orchard images is important for yield prediction, fruit counting, robotic harvesting, and crop monitoring. However, changing illumination, leaf clutter, dense fruit clusters, and partial occlusion make detection difficult. To provide a fair and reproducible comparison, this study establishes a controlled benchmark for single-class apple detection on the public AppleBBCH81 dataset using one deterministic train, validation, and test split and a unified evaluation protocol across six representative detectors: YOLOv10n, YOLO11n, RT-DETR-L, Faster R-CNN (ResNet50-FPN), FCOS (ResNet50-FPN), and SSDLite320 (MobileNetV3-Large). Performance is evaluated primarily using COCO-style mAP@0.5 and mAP@0.5:0.95, and threshold-dependent behavior is further analyzed using precision-recall curves and fixed-threshold precision, recall, and F1-score at IoU = 0.5. On the validation split, YOLO11n achieves the best strict localization performance with mAP@0.5:0.95 = 0.6065 and mAP@0.5 = 0.9620, followed closely by RT-DETR-L and YOLOv10n. At a fixed operating point with confidence >= 0.05, YOLOv10n attains the highest F1-score, whereas RT-DETR-L achieves very high recall but low precision because of many false positives at low confidence. These findings show that detector selection for orchard deployment should be guided not only by localization-aware accuracy but also by threshold robustness and the requirements of the downstream task.

---

## 论文详细总结（自动生成）

以下是对该论文的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在精准农业中，苹果检测是产量预测、果实计数、机器人采摘和作物监测的基础任务。然而，真实的果园环境极其复杂，存在光照剧烈变化、枝叶遮挡、果簇密集等问题。
*   **核心问题**：尽管深度学习在果园目标检测中取得了显著进展，但现有研究往往使用不同的数据集、数据划分方法、模型架构和评估指标，导致难以对现代目标检测器进行公平、客观的横向比较。此外，许多研究仅报告 `mAP@0.5`，掩盖了模型在更严格定位要求下的缺陷，也未能反映模型在不同置信度阈值下的实际部署表现。
*   **整体含义**：本文旨在建立一个受控的、可重复的标准化基准（Benchmark），在统一的训练/测试划分和评估协议下，公平对比六种不同架构的现代目标检测器在果园图像中的表现，从而为实际农业部署提供指导。

### 2. 论文提出的方法论
*   **核心思想**：本文并未提出新的网络架构，而是提出了一套**标准化的评估工作流**。通过统一数据处理、固定数据集划分、对齐训练配置，并结合严格的定位指标与面向部署的阈值指标，来全面评估检测器。
*   **关键技术细节与评估流程**：
    *   **任务定义**：单类别（苹果）目标检测。
    *   **一对一匹配规则（One-to-One Matching Rule）**：在计算特定置信度下的真阳性（TP）、假阳性（FP）和假阴性（FN）时，将预测框按置信度降序排列，每个预测框贪婪匹配具有最高 IoU（且 $\ge 0.5$）的未匹配真实框。这有效防止了密集果簇中对同一苹果的重复计数。
    *   **双重视角评估**：
        1.  **定位感知指标**：使用 COCO 标准的 `mAP@0.5` 和 `mAP@0.5:0.95` 评估模型在整个置信度区间内的综合排序和定位精度。
        2.  **阈值依赖指标（面向部署）**：在固定的工作点（如 IoU = 0.5 且 置信度 $\ge 0.05$）下，计算精确率（Precision）、召回率（Recall）、F1 分数，并绘制 PR 曲线和计算 PR AUC，以评估模型在实际应用中拒绝误检的能力。

### 3. 实验设计
*   **数据集**：使用公开的果园数据集 **AppleBBCH81**（YOLO 格式），包含 1,838 张图像和 15,309 个苹果边界框（包含 16 张无苹果的负样本图像以减少假阳性偏差）。
*   **数据划分（Benchmark）**：采用单一且确定性的划分方式（约 80/10/10），即 1,470 张训练集、183 张验证集、185 张测试集。所有结果均在验证集上报告。
*   **对比方法（6种代表性模型）**：
    1.  **YOLOv10n**：现代轻量级单阶段 CNN。
    2.  **YOLO11n**：最新的 Ultralytics 单阶段检测器。
    3.  **RT-DETR-L**：基于 Transformer 的实时检测器。
    4.  **Faster R-CNN (ResNet50-FPN)**：经典双阶段基线模型。
    5.  **FCOS (ResNet50-FPN)**：无锚框（Anchor-free）单阶段基线模型。
    6.  **SSDLite320 (MobileNetV3-Large)**：面向边缘设备部署的超轻量级基线模型。

### 4. 资源与算力
*   **硬件算力**：实验在 Google Colab 环境下进行，使用了一张 **NVIDIA A100-SXM4-40GB GPU**。
*   **软件环境**：Python 3.12.12, PyTorch 2.10.0+cu128, CUDA 12.8。
*   **训练时长与配置**：
    *   Ultralytics 模型（YOLOv10n, YOLO11n, RT-DETR-L）：训练 50 个 Epochs，Batch size 为 16，输入分辨率 640x640。
    *   Torchvision 模型（Faster R-CNN, FCOS, SSDLite320）：训练 10 个 Epochs，Batch size 为 4，输入分辨率 800-1333（SSDLite 为 320）。
    *   *注：文中未明确说明具体的训练耗时（小时/分钟），但详细记录了 Epoch 数量和超参数。*

### 5. 实验数量与充分性
*   **实验数量**：对 6 种模型在固定的数据集划分上进行了完整的训练和验证。评估包含了综合 mAP 对比、固定阈值下的混淆矩阵（TP/FP/FN）统计、F1 分数对比以及 PR 曲线分析。
*   **客观性与公平性**：**非常公平且客观**。作者严格控制了随机种子（seed=0），统一了数据预处理逻辑，保留了负样本图像，并使用了完全一致的评估代码。
*   **充分性**：在单一数据集上的横向对比是充分的，但缺乏跨数据集（不同果园、不同季节）的泛化性实验，也未进行消融实验。

### 6. 论文的主要结论与发现
*   **定位精度最高**：在严格的 COCO 指标下，**YOLO11n** 表现最好（`mAP@0.5:0.95` = 0.6065，`mAP@0.5` = 0.9620），RT-DETR-L 和 YOLOv10n 紧随其后。
*   **实际部署（固定阈值）表现最佳**：在置信度 $\ge 0.05$ 的实际工作点下，**YOLOv10n** 实现了精确率和召回率的最佳平衡，获得了最高的 F1 分数（0.780870）。
*   **高 mAP 不等于高可用性**：RT-DETR-L 虽然 mAP 很高且召回率极高（0.970218），但在低置信度下会产生海量的假阳性（FP=5061），导致精确率极低（0.224605）。YOLO11n 的误检也多于 YOLOv10n。
*   **核心指导意义**：果园检测器的选择不能仅看 mAP，必须结合下游任务。例如，果实计数任务对假阳性（重复计数）敏感，应选择 F1 更高的模型；而机器人采摘更看重定位精度和高召回率。

### 7. 优点
*   **高度的标准化与可重复性**：打破了以往农业 CV 论文“自说自话”的局面，提供了一个严格控制变量的 Benchmark。
*   **评估视角的实用性**：引入了面向实际部署的“固定阈值评估”和“一对一匹配规则”，深刻揭示了模型在真实果园应用中可能遇到的“重复检测”和“低置信度误检”问题。
*   **模型覆盖面广**：涵盖了单阶段、双阶段、无锚框、Transformer 以及轻量级等当前主流的所有目标检测架构。

### 8. 不足与局限
*   **数据集单一**：仅在 AppleBBCH81 一个数据集上进行了测试，缺乏跨果园、跨季节、跨苹果品种的泛化能力验证。
*   **缺乏端侧性能测试**：虽然论文强调了“部署”，但并未实际测试各模型在边缘设备（如树莓派、Jetson Nano）上的推理延迟（FPS）、内存占用和模型大小。
*   **任务局限性**：仅限于 2D 边界框检测。对于实际的机器人采摘，通常还需要实例分割（判断抓取点）或 3D 深度定位，本文未涉及这些延伸任务。

（完）
